build:
  docker:
    front: ./front/Dockerfile
    api: ./api/Dockerfile

run: 
  translate-front: 
    command:
      - npm run dev
    image: front
  translate-api: 
    command:
      - api/translate-memo-api
    image: api