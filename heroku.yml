build:
  docker:
    front: ./front/Dockerfile
    api: ./api/Dockerfile

run: 
  translate-front: 
    command:
      - npm run dev
    image: front
  translate-api: 
    command:
      - ./translate-memo-api
    image: api