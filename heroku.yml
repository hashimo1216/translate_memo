build:
  docker:
    front: ./front/Dockerfile
    api: ./api/Dockerfile

run: 
  translate-front: 
    command:
      - npm run start
    image: registry.heroku.com/translate-memo/front:latest
  translate-api: 
    command:
      - api/translate-memo-api
    image: translatememo-api:latest